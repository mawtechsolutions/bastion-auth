# ============================================
# BastionAuth - Production Environment
# ============================================
# 
# Copy this file to .env and fill in the values
# cp env.example .env
#
# ============================================

# ==========================================
# Database Configuration
# ==========================================
DB_USER=bastionauth
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DB_NAME=bastionauth

# ==========================================
# Redis Configuration
# ==========================================
REDIS_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# ==========================================
# JWT Keys (RS256)
# ==========================================
# Generate with:
#   openssl genrsa -out private.pem 2048
#   openssl rsa -in private.pem -pubout -out public.pem
#   base64 -w 0 private.pem > private.b64
#   base64 -w 0 public.pem > public.b64
#
# Then paste the base64 encoded values here (single line, no newlines)
JWT_PRIVATE_KEY=BASE64_ENCODED_PRIVATE_KEY_HERE
JWT_PUBLIC_KEY=BASE64_ENCODED_PUBLIC_KEY_HERE

# ==========================================
# Encryption Key
# ==========================================
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=GENERATE_32_BYTE_HEX_KEY_HERE

# ==========================================
# Email Configuration (SMTP)
# ==========================================
# Gmail example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASSWORD=your-app-password
#
# Other providers:
#   Mailgun, SendGrid, Amazon SES, etc.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@bastionauth.dev

# ==========================================
# OAuth Providers (Optional)
# ==========================================
# Google OAuth
# Get credentials at: https://console.cloud.google.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth
# Get credentials at: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ==========================================
# Application Keys
# ==========================================
# Publishable key for frontend SDK
PUBLISHABLE_KEY=pk_live_bastionauth

# ==========================================
# URLs (Already configured for bastionauth.dev)
# ==========================================
# These are set in docker-compose.prod.yml
# API_URL=https://api.bastionauth.dev
# FRONTEND_URL=https://app.bastionauth.dev
# ADMIN_URL=https://admin.bastionauth.dev

